let firstName = "Minato";
let lastName = "Aqua";

const fullName = firstName + " " + lastName;
console.log("Full Name (using +): " + fullName);

const fullNameWithLiteral = `${firstName} ${lastName}`;
console.log("Full Name (using template literal): " + fullNameWithLiteral);
